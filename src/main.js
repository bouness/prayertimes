import React from 'react'
import ReactDOM from 'react-dom/client'
import PrayerTimesApp from './App'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PrayerTimesApp />
  </React.StrictMode>,
)